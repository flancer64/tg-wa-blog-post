# Резюме изменений

Unit-тесты переведены с тестового DI-контейнера на прямую конструкторную инъекцию зависимостей. Файл `test/unit/unit-bootstrap.mjs` и связанный нормативный документ удалены, а testing-контракт обновлён под новую модель unit-тестирования.

# Детали работ

- Переписаны все unit-тесты в `test/unit/back/` на прямой импорт production-классов и явную передачу моков через конструктор.
- Удалён `test/unit/unit-bootstrap.mjs` как лишний слой между тестом и тестируемым классом.
- Обновлены `test/AGENTS.md`, `ctx/docs/code/testing.md` и `ctx/docs/code/AGENTS.md`, чтобы unit-зона была описана как прямое конструирование классов без отдельного bootstrap.
- Удалён устаревший документ `ctx/docs/code/testing/unit-bootstrap.md`.

# Результаты

- Подтверждена исходная гипотеза: для текущих unit-тестов контейнер не нужен, поскольку зависимости уже внедряются через конструктор.
- Выполнена проверка `npm run test:unit`: 12 тестов, 12 passed, 0 failed.
- Дополнительно зафиксировано, что `npm test` в проекте не настроен и не использовался как нормативная команда проверки.
