# Отчёт итерации

Path: `ctx/agent/report/2026/03/06/13-44-readme-run-modes.md`

## Резюме изменений

README проекта обновлён и теперь отражает актуальные варианты запуска приложения, включая аварийный ручной режим через `--source-file`.

## Детали работ

В `README.md` удалено устаревшее упоминание проверки интеграции через отсутствующую команду `npm run tg:check`.

В `README.md` добавлены разделы со штатным запуском через `npm start` и `node bin/bootstrap.mjs`, а также с аварийным ручным запуском через `node bin/bootstrap.mjs --source-file ./var/manual/post.txt`.

В README также зафиксированы требования к входному файлу ручного режима и команда запуска unit-тестов `npm run test:unit`.

## Результаты

Пользовательская документация теперь описывает все актуальные варианты запуска приложения на уровне входной точки проекта.

Тесты не запускались, поскольку итерация ограничена обновлением документации.
